---
title: Bezier
---

# {{bezier}}

An <Type>array</Type> of three or four <Type>vector</Type>s, the start point, the endpoint, the first control point and optionally a second control point if the bezier is cubic.

The following functions are in the `bezier` module.

import Combined from "@site/cetz/docs/_generated/bezier/-combined.mdx";

<Combined />
